# Radiolaria-DINOv3 主实验运行清单（可直接照做）

> 这份清单是给服务器跑实验用的「一步一步手册」。
> 按顺序执行即可，先最小验证，再全量跑。

---

## 0. 你现在要跑的是什么？

本项目新增了 3 个实验脚本：

1. `scripts/few_shot_classifier.py`（主实验，最重要）
2. `scripts/ablation_robustness.py`（消融 + 鲁棒性）
3. `scripts/one_shot_extreme.py`（1-shot 极限测试）

公共工具与配置：

- `scripts/exp_utils.py`
- `configs/experiments/few_shot_classifier.yaml`
- `configs/experiments/ablation_robustness.yaml`
- `configs/experiments/one_shot_extreme.yaml`

---

## 1. 运行前检查（必须）

### 1.1 进入项目目录

```bash
cd /home/user/shenyaxuan/Radiolaria-DINOv3
```

### 1.2 激活环境

```bash
conda activate CV   # 本机环境名（原清单写的 dinov3 不存在）
```

### 1.3 安装依赖（建议再执行一次）

```bash
pip install -r requirements.txt
pip install openpyxl seaborn
```

> `openpyxl` 用于导出 Excel，缺了会报错。

### 1.4 数据目录检查

需要至少有：

- `data/images_supervised/train`
- `data/images_supervised/val`
- `data/images_supervised/test`
- `data/class_mapping.csv`

---

## 2. checkpoint 怎么填（最关键）

打开这 3 个 yaml，把 `checkpoints` 改成你服务器真实路径：

- `configs/experiments/few_shot_classifier.yaml`
- `configs/experiments/ablation_robustness.yaml`
- `configs/experiments/one_shot_extreme.yaml`

### 推荐填写规则

- `0%`：官方 ImageNet-22k 初始权重
- `20/50/80/100%`：对应 TAPT 训练后的权重

你给的这种路径可以直接用（推荐）：

`/home/user/shenyaxuan/Radiolaria-DINOv3/outputs/tapt_vits16_50/eval/training_37499/teacher_checkpoint.pth`

> 脚本已支持 `teacher_checkpoint.pth`、`checkpoint.pth`、`teacher/model` 封装格式。

---

## 3. 先跑最小验证（强烈建议）

先验证一组最小任务，不要一上来跑全量。

```bash
python scripts/few_shot_classifier.py \
  --config configs/experiments/few_shot_classifier.yaml \
  --tapt_fraction 100 \
  --model_size base \
  --k_shot 5
```

成功后检查输出目录：

- `results/few_shot_classifier/run.log`
- `results/few_shot_classifier/few_shot_seed_results.csv`
- `results/few_shot_classifier/few_shot_summary.csv`
- 各 seed 子目录中的 confusion matrix 和 attention 图

---

## 4. 主实验（全量 few-shot）

```bash
python scripts/few_shot_classifier.py \
  --config configs/experiments/few_shot_classifier.yaml \
  --run_full_grid
```

这会遍历：

- TAPT 比例：0/20/50/80/100
- 模型：small/base/large
- K-shot：1/3/5/10
- 种子：42/123/456/789/999

输出：

- CSV + Markdown + Excel 汇总
- 混淆矩阵（PNG+PDF）
- attention rollout-like 可视化（PNG+PDF）

---

## 5. 消融 + 鲁棒性实验

```bash
python scripts/ablation_robustness.py \
  --config configs/experiments/ablation_robustness.yaml
```

输出：

- `ablation_seed_results.csv`
- `ablation_summary.csv/.md/.xlsx`
- `ablation_table.tex`（可直接贴论文）

### 关于 cross-domain

如果你要启用 cross-domain，请先准备：

- `data/domain_split.csv`

格式至少两列：

```csv
filepath,domain
img001.jpg,south_china
img002.jpg,japan
```

> `domain` 需包含关键字 `south` 或 `japan`。

如果没有这个文件，脚本会跳过 cross-domain 结果（不影响其余实验）。

---

## 6. 1-shot 极限实验

```bash
python scripts/one_shot_extreme.py \
  --config configs/experiments/one_shot_extreme.yaml \
  --model_size base

python scripts/one_shot_extreme.py \
  --config configs/experiments/one_shot_extreme.yaml \
  --model_size large
```

输出：

- `one_shot_seed_results.csv`
- `one_shot_summary.csv/.md/.xlsx`
- `failure_cases/failure_cases_albaillellaria.png/pdf`

---

## 7. 一键全跑（脚本已给）

```bash
bash scripts/run_all.sh
```

> 注意：这会跑很久，建议先完成第 3 步最小验证。

---

## 8. 后台运行建议（服务器）

### 用 nohup

```bash
nohup python scripts/few_shot_classifier.py \
  --config configs/experiments/few_shot_classifier.yaml \
  --run_full_grid > logs/few_shot_full.log 2>&1 &
```

### 查看日志

```bash
tail -f logs/few_shot_full.log
```

### 查看 GPU

```bash
watch -n 1 nvidia-smi
```

---

## 9. 常见报错与处理

### 报错：找不到 checkpoint
- 检查 yaml 里的绝对路径是否存在。
- 路径不要写错模型尺寸（small/base/large 对应维度不同）。

### 报错：`openpyxl` not found
- 执行：`pip install openpyxl`

### 报错：cross-domain 为空
- 检查 `data/domain_split.csv` 是否存在且格式正确。

### 结果目录太大
- 可先只跑 `--tapt_fraction 100 --model_size base --k_shot 5` 验证。

---

## 10. 你可以直接复制执行的推荐顺序

```bash
# 1) 最小验证
python scripts/few_shot_classifier.py --config configs/experiments/few_shot_classifier.yaml --tapt_fraction 100 --model_size base --k_shot 5

# 2) 主实验全量
python scripts/few_shot_classifier.py --config configs/experiments/few_shot_classifier.yaml --run_full_grid

# 3) 消融+鲁棒性
python scripts/ablation_robustness.py --config configs/experiments/ablation_robustness.yaml

# 4) 1-shot
python scripts/one_shot_extreme.py --config configs/experiments/one_shot_extreme.yaml --model_size base
python scripts/one_shot_extreme.py --config configs/experiments/one_shot_extreme.yaml --model_size large
```

---

如果你愿意，我下一步可以帮你再生成一份「只跑 Base+Large 的节省算力版配置」。
